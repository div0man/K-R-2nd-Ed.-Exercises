/* Exercise 1-7. Write a program to print the value of EOF.
 */

#include <stdio.h>

main()
{
	printf("EOF as decimal integer:     %d\n", EOF);
	printf("EOF as hexadecimal integer: %x\n", EOF);
	printf("EOF as octal integer:       %o\n", EOF);
	printf("EOF as character:           %c\n", EOF);
}
